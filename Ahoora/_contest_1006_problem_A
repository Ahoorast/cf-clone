#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long n, arr[11000];
	cin >> n;
	for(int i = 0; i < n; i++)
	cin >> arr[i];
	for(int i = 0; i < n; i++)
	if(arr[i] % 2 == 0)
	arr[i]--;
	for(int i = 0; i < n; i++)
	cout << arr[i] << " ";
	return 0;
}
