#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, m, k, ans = 0;
	cin >> n >> m >> k;
	for(int i = 0; i < k; i++)
	{
		ans += 2 * (n + m)  - 4;
		n -= 4;
		m -= 4;
	}
	cout << ans;
	return 0;
}
