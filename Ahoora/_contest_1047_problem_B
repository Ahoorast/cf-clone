#include <bits/stdc++.h>
using namespace std;
 
int main() {
 	ios_base:: sync_with_stdio(false), cin.tie(0), cout.tie(0);
	long long res = 0, n, x, y;
	cin >> n;
	while (n--)
		cin >> x >> y, res = max(res, x + y);
	cout << res << endl;
	return 0;
}
