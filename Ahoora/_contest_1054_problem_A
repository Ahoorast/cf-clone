#include <bits/stdc++.h>
using namespace std;
 
int main()
{
	long long x, y, z, t1, t2, t3, s, e;
	cin >> x >> y >> z >> t1 >> t2 >> t3;
	s = abs(x - y) * t1;
	e = abs(z - x) * t2 + t3 + t3 + abs(x - y) * t2 + t3;
	if(s >= e) cout << "YES";
	else cout << "NO";
	return 0;
}
