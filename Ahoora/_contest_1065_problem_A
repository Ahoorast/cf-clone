#include <bits/stdc++.h>
using namespace std;
int main()
{
	unsigned long long q, s, a, b, ans = 0, c;
	cin >> q;
	for(int i = 0; i < q; i++)
	{
		cin >> s >> a >> b >> c;
		s /= c;
		cout << (s / a) * b + s << endl;
	}
	return 0;
}
