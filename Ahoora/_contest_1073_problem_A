#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	int n;
	string s;
	cin >> n >> s;
	for (int i = 1; i < n; i++)
		if (s[i] != s[i - 1])
			return cout << "YES" << endl << s[i - 1] << s[i] << endl, 0;
	cout << "NO" << endl;
	return 0;
}
