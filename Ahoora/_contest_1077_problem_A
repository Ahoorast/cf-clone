#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long ans = 0, n, a, b , t;
	cin >> n;
	for(int i = 0; i < n; i++)
	{
		cin >> a >> b >> t;
		if(t % 2 == 0)
		cout << (a - b)  * (t / 2) << endl;
		else
		cout << (a - b) * ((t - 1) / 2) + a << endl;
	}
	return 0;
}
