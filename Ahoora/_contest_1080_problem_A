#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long n, a, ans = 0;
	cin >> n >> a;
	long long a1 = n * 2, a2 = n * 5, a3 = n * 8;
	ans += a1 / a;
	if(a1 % a != 0)
	ans++;
	ans += a2 / a;
	if(a2 % a != 0)
	ans++;
	ans += a3 / a;
	if(a3 % a != 0)
	ans++;
	cout << ans;
	return 0;
}
