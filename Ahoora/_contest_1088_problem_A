#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, a, b;
	cin >> n;
	for(int i = n; i >= 1; i--)
	for(int j = i; j >= 1; j--)
	if(i % j == 0 && i * j > n && i / j < n)
	{
		cout << i << " " << j;
		return 0; 
	}
	cout << "-1";
	return 0;
}
