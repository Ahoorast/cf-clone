#include <bits/stdc++.h>
using namespace std;
int main()
{
	int x, y, z, a, b, c, sum = 0;
	cin >> x >> y >> z >> a >> b >> c;
	a -= x;
	if(a < 0)
	{
		cout << "NO";
		return 0;
	}
	sum = a + b;
	sum -= y;
	if(sum < 0)
	{
		cout << "NO";
		return 0;
	}
	sum += c;
	sum -= z;
	if(sum < 0)
	{
		cout << "NO";
		return 0;
	}
	cout << "YES";
	return 0;
}
