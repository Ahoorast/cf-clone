#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long n, ans = 0;
	char a;
	cin >> n;
	for(int i = 0; i < n; i++)
	{
		cin >> a;
		if((a - '0') % 2 == 0)
		ans += i + 1; 
	}
	cout << ans;
	return 0;
}
