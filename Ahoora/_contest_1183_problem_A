#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long a, b;
	cin >> a;
	for(int i = a; true; i++)
	{
		b = i;
		int sum = 0;
		while(b)
		{
			sum += b % 10;
			b /= 10;
		}
		if(sum % 4 == 0)
		{
			cout << i;
			return 0;
		}
	}
	return 0;
}
