#include <bits/stdc++.h>
using namespace std;
int main()
{
    long double h, l;
    cin >> h >> l;
    cout << setprecision(13) << fixed;
    cout << ((l * l) - (h * h)) / (2 * h) << endl;
    return 0;
}
