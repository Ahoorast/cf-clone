#include <bits/stdc++.h>
using namespace std;
 
const int x = 1378;
 
int main() {
 	ios_base:: sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int a, b, n, res = 0;
	cin >> n >> a >> b;
	for (int i = 1; i <= n; i++)
		if (i - 1 <= b && n - i >= a)
			res++;
	cout << res << endl;
	return 0;
}
