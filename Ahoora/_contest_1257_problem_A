#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		int n, x, a, b;
		cin >> n >> x >> a >> b;
		cout << min(n - 1, abs(a - b) + x) << endl;
	}
	return 0;
}
