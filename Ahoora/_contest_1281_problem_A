#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		string s;
		cin >> s;
		int sz = s.size();
		if (s.substr(sz - 2) == "po")
			cout << "FILIPINO" << endl;
		else if (s.substr(sz - 4) == "desu" || s.substr(sz - 4) == "masu")
			cout << "JAPANESE" << endl;
		else if (s.substr(sz - 5) == "mnida")
			cout << "KOREAN" << endl;
	}
	return 0;
}
