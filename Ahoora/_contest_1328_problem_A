#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		long long a, b;
		cin >> a >> b;
		long long t = (a + b - 1) / b;
		cout << t * b - a << '\n';
	}
	return 0;
}
