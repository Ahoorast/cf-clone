#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		long long n;
		cin >> n;
		if (n & 1)
			cout << n / 2 << endl;
		else
			cout << (n - 2) / 2 << endl;
	}
	return 0;
}
