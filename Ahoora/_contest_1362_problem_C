#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		long long n;
		unsigned long long ans = 0;
		cin >> n;
		while (n) {
			ans += n;
			n /= 2;
		}
		cout << ans << '\n';
	}
	return 0;
}
