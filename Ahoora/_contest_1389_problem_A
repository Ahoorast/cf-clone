#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		int l, r;
		cin >> l >> r;
		if (l * 2 <= r)
			cout << l << ' ' << 2 * l << '\n';
		else
			cout << "-1 -1\n";
	}
	return 0;
}
