#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		long long n, m, x;
		cin >> n >> m >> x;
		x--;
		long long j = x / n, i = x % n;
		cout << m * i + j + 1 << '\n';
	}
	return 0;
}
