#include <iostream>
using namespace std;
int main()
{
	int k, n, arr[200], ans = 0;
	cin >> n >> k;
	for(int i = 0; i < n; i++)
	cin >> arr[i];
	for(int i = 0; i < n; i++)
	if(arr[k - 1] <= arr[i] && arr[i] > 0)
	ans++;
	cout << ans;
	return 0;
}
