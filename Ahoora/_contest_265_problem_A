#include <bits/stdc++.h>
using namespace std;
int main()
{
	string s, t;
	cin >> s >> t;
	int sz = t.size(), k = 0, l = 1, tz = s.size();
	for(int i = 0; i < sz; i++)
	if(s[l - 1] == t[i])
	l++;
	cout << min(l, tz);
	return 0;
}
