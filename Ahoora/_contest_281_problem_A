#include <iostream>
using namespace std;
int main()
{
	string a;
	int az = a.size();
	cin >> a;
	if(a[0] >= 'A' && a[0] <= 'Z')
	cout << a;
	else
	{
		a[0] -= 'z' - 'a' + 7;
		;
		cout << a;
	}
	return 0;
}
