#include <iostream>
using namespace std;
int main()
{
	unsigned long long n, k, ans = 0;
	cin >> n >> k;
	if(k <= (n + 1) / 2) // t frd
	{
		cout << (k - 1) * 2 + 1;
		return 0;
	}
	k = k - (n + 1) / 2;
	cout << 2 * k;
	return  0;
}
