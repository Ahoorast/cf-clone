#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, x, sum = 0, a, ans = 0;
	cin >> n >> x;
	for(int i = 0; i < n; i++)
	{
		cin >> a;
		sum += a;
	}
	ans = abs(sum) / x;
	if(abs(sum) % x != 0)
	ans++;
	cout << ans;
	return 0;
}
