#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, k, ans = 0, a;
	cin >> n >> k;
	for(int i = 0; i < n; i++)
	{
		cin >> a;
		if(5 - a >= k)
		ans++;
	}
	cout << ans / 3;
	return 0;
}
