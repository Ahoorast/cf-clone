#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long ans = 0, sum = 0, n;
	cin >> n;
	for(int i = 1; sum < n; i++)
	{
		sum += (i * (i + 1)) / 2;
		ans++;
	}
	if(sum == n)
	ans++;
	cout << ans - 1;
	return 0;
}
