#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, a, b;
	cin >> a >> b >> n;
	for(int i = n / a; i > -1; i--)
	if((n - (i * a)) % b == 0)
	{
		cout << "Yes";
		return 0;
	}
	cout << "No";
	return 0;
}
