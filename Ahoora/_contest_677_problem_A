#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, h, a;
	long long ans = 0;
	cin >> n >> h;
	for(int i = 0; i < n; i++)
	{
		cin >> a;
		if(a > h)
		ans += 2;
		else
		ans += 1;
	}
	cout << ans;
	return 0;
}
