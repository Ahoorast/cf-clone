#include <bits/stdc++.h>
using namespace std;
 
const int maxn = 200;
int arr[maxn];
 
int main() {
 	ios_base:: sync_with_stdio(false), cin.tie(0), cout.tie(0);
	long long n, k, tmp;
	cin >> n >> k;
	tmp = k - n % k;
	if (tmp == 0) tmp = n;
	cout << n + tmp;
	return 0;
}
