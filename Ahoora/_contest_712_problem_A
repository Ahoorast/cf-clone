#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e6 + 10;
int main()
{
	long long n, arr[maxn];
	cin >> n;
	for(int i = 0; i < n; i++)
	cin >> arr[i];
	arr[n] = 0;
	for(int i = 0; i < n; i++)
	cout << arr[i] + arr[i + 1] << ' ';
	return 0;
}
