#include <bits/stdc++.h>
using namespace std;
 
const int N = 1000 * 1000 * 1000 + 100;
 
int main() {
	string s, t;
	cin >> s >> t;
	int sz = s.size(), tz = t.size();
	if (s != t)
		return cout << max(tz, sz), 0;
	cout << "-1";
	return 0;
}
