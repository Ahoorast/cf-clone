#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int n;
	string s, t = "aabb";
	cin >> n;
	for (int i = 0; i < n / 4; i++)
		cout << t;
	for (int i = 0; i < n % 4; i++)
	 	cout << t[i];
	return 0;
}
