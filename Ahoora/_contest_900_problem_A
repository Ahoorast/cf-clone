#include <bits/stdc++.h>
using namespace std;
 
int main() {
	long long n, l = 0, r = 0, x, y;
	cin >> n;
	while (n--) {
		cin >> x >> y;
		if (x > 0)
			r++;
		else 
			l++;
	}
	if (r == 1 || l == 1 || r == 0 || l == 0)
		return cout << "YES", 0;
	cout << "NO";
	return 0;
}
