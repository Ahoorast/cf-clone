#include <bits/stdc++.h>
using namespace std;
 
vector<int> ans;
 
int main() {
	ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0); 
	long long n, m, k = 1;
	cin >> n >> m;
	while (n && k <= m)
		k *= 2, n--;
	cout << m % k;
	return 0;
}
