#include <bits/stdc++.h>
using namespace std;
 
int main()
{
	int n, m, x;
	cin >> n >> m;
	if(n < m) swap(n, m);
	x = (n - m) / 2 + m;
	cout << ((n - x) * (n - x + 1) / 2) + ((x - m) * (x - m + 1)) / 2;	
	return 0;
}
