#include <iostream>
using namespace std;
int main()
{
	int n, m, ans = 0;
	cin >> n >> m;
	string s, a;
	ans = n % m;
	if(ans == 0)
	ans = m;
	getline(cin, s);
	for(int i = 1; i <= m; i++)
	{
		getline(cin, s);
		if(ans == i)
		a = s;
	}
	cout << a << endl;
	return 0;
}
