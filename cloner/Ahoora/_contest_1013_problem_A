#include <bits/stdc++.h>
using namespace std;
const int maxn = 1100;
int x[maxn], y[maxn];
int main()
{
	int n, a, b, sum1 = 0, sum2 = 0;
	cin >> n;
	for(int i = 0; i < n; i++) {
		cin >> a;
		sum1 += a;
	}
	for(int i = 0; i < n; i++) {
		cin >> b;
		sum2 += b;
	}
	if(sum1 < sum2) cout << "No";
	else cout << "Yes";
	return 0;
}
