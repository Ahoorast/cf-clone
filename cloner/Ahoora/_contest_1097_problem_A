#include <bits/stdc++.h>
using namespace std;
int main()
{
	string a, s;
	int cnt = 0;
	cin >> a;
	for(int i = 0; i < 5; i++)
	{
		cin >> s;
		if(s[0] == a[0] || s[1] == a[1])
		cnt++;
	}
	if(cnt > 0)
	cout << "YES";
	else
	cout << "NO";
	return 0;
}
