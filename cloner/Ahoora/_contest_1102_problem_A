#include <bits/stdc++.h>
using namespace std;
 
int main()
{
	long long n;
	cin >> n;
	if(n % 2 == 0) {
		n /= 2;
		if(n % 2 == 0) cout << '0';
		else cout << '1';
	}
	else if(n % 2 == 1) {
		n++;
		n /= 2;
		if(n % 2 == 1) cout << '1';
		else cout << '0';
	}
	return 0;
}
