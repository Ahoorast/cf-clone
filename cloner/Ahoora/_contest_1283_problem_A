#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		int h, m;
		cin >> h >> m;
		cout << 60 - m + max(0, 24 - h - 1) * 60 << endl;
	}
	return 0;
}
