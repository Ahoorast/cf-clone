#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		long long a, b, c, d;
		cin >> a >> b >> c >> d;
		cout << b << ' ' << c << ' ' << c << endl;	
	}
	return 0;
}
