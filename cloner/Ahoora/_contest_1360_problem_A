#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	int Q;
	cin >> Q;
	while (Q--) {
		int a, b;
		cin >> a >> b;
		cout << pow(min({max(a * 2, b), max(b * 2, a), a + b}), 2) << '\n';
	}
	return 0;
}
