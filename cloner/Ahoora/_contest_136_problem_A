#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, arr[1000], a;
	cin >> n;
	for(int i = 0; i <= n; i++)
	arr[i] = 0;
	for(int i = 0; i < n; i++)
	{
		cin >> a;
		arr[a - 1] = i + 1;
	}
	for(int i = 0; i < n; i++)
	cout << arr[i] << " ";
	return 0;
}
