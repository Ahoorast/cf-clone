#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int Q;
	cin >> Q;
	while (Q--) {
		int a, b, n;
		cin >> n >> a;
		for (int i = 1; i < n; i++)
			cin >> b;
		cout << (a < b? "YES": "NO") << '\n';
	}
	return 0;
}
