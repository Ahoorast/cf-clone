#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	int Q;
	cin >> Q;
	while (Q--) {
		string s;
		cin >> s;
		int x = s[0] - '0', sz = (int)(s.size());
		int ans = 10 * (x - 1);
		for (int i = 1; i <= sz; i++)
			ans += i;
		cout << ans << '\n';
	}
	return 0;
}
