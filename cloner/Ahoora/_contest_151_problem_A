#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, k, l, c, d, p, nl, np, ans;
	cin >> n >> k >> l >> c >> d >> p >> nl >> np;
	ans = min(min((k * l) / nl, c * d), min(c * d, p / np));
	cout << ans / n;
	return 0;
}
