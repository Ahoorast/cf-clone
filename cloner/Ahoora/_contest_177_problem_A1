#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, a, ans = 0;
	cin >> n;
	for(int i = 0; i < n; i++)
	for(int j = 0; j < n; j++)
	{
		cin >> a;
		if(i == n / 2 || j == n / 2 || i == j || j + i == n - 1)
		ans += a;	
	}
	cout << ans;
	return 0;
}
