#include <bits/stdc++.h>
using namespace std;
int main()
{
	long double ans = 0, n, p;
	cin >> n;
	for(int i = 0; i < n; i++)
	{
		cin >> p;
		ans += p;
	}
	ans /= n;
	cout << setprecision(12) << fixed;
	cout << ans;
	return 0;
}
