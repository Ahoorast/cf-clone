#include <bits/stdc++.h>
using namespace std;
int main()
{
	string a;
	cin >> a;
	int az = a.size();
	for(int i = 0; i < az; i++)
	{
		if(a[i] == 'W' && a[i + 1] == 'U' && a[i + 2] == 'B')
		{
			a[i] = ' ';
			a[i + 1] = ' ';
			a[i + 2] = ' ';
			i = 0;
		}
	}
	cout << a;
	return 0;
}
