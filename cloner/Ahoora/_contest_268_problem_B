#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	long long n, res = 0;
	cin >> n; 
	for (int i = 0; i < n; i++)
		res += (n - i) * (i);
	cout << res + n << endl;
	return 0;
}
