#include <iostream>
using namespace std;
int main()
{
	int a, b, used = 0, ans = 0;
	cin >> a >> b;
	while(a)
	{
		used++;
		ans++;
		a--;
		if(used == b)
		{
			a++;
			used = 0;
		}
	}
	cout << ans;
	return 0;
}
