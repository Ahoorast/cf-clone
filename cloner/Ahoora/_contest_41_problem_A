#include <iostream>
using namespace std;
int main()
{
	string a, b;
	cin >> a >> b;
	int az = a.size();
	for(int i = 0; i < az; i++)
	{
		if(a[i] != b[i])
		cout << "1";
		else
		cout << "0";
	}
	return 0;
}
