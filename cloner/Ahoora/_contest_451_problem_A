#include <iostream>
using namespace std;
int main()
{
	int n, m, small;
	cin >> n >> m;
	if(n >= m)
	small = m;
	else
	small = n;
	if(small % 2 == 0)
	cout << "Malvika";
	else
	cout << "Akshat";	
	return 0;
}
