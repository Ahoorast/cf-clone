#include <iostream>
using namespace std;
int main()
{
	long long c1, c2, c3, c4, c5, c = 0;
	cin >> c1 >> c2 >> c3 >> c4 >> c5;
	c = c1 + c2 + c3 + c4 + c5;
	if(c == 0)
	{
		cout << "-1";
		return 0;
	}
	if(c % 5 != 0)
	{
		cout << "-1";
		return 0;	
	}
	cout << c / 5;
	return 0;
}
