#include <iostream>
using namespace std;
int main()
{
	long long m, n, ans = 0;
	cin >> m >> n;
	if(m > n)
	swap(m, n);
	ans += n * (m / 2);
	if(m % 2 == 1)
	ans += n / 2;
	cout << ans;
	return  0;
}
