#include <bits/stdc++.h>
using namespace std;
 
const int maxn = 1e5 + 100;
long long a[2][maxn], n[2], k, m;
 
int main() {
 	ios_base:: sync_with_stdio(false), cin.tie(0), cout.tie(0);
	long long a, b, n, s;
	cin >> a >> b >> n;
	if (abs(a) + abs(b) > n) return cout << "No", 0;
	if ((abs(a) + abs(b)) % 2 != n % 2) return cout << "No", 0;
	cout << "Yes" << endl;
	return 0;
}
