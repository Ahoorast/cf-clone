#include <bits/stdc++.h>
using namespace std;
int main()
{
	long long n, x, ans = 0;
	cin >> n >> x;
	for(int i = 1; i <= min(n, x); i++)
	if(x % i == 0 && x / i <= n)
	ans++;
	cout << ans;
	return 0;
}
