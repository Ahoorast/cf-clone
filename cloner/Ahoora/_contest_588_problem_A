#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0); 
	int n, a, p, res = 0, minx = 230;
	cin >> n;
	while (n--) {
		cin >> a >> p;
		minx = min(minx, p);
		res += minx * a;
	}
	cout << res << endl;
	return 0;
}
