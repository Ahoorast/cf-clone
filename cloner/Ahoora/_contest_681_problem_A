#include <bits/stdc++.h>
int arr[200];
using namespace std;
int main()
{
	string s;
	long long l, n, ans = 0, r, sz;
	cin >> n;
	for(int i = 0; i < n; i++)
	{
		cin >> s >> l >> r;
		if(l >= 2400 && r > l)
		ans++;
	}
	if(ans == 0)
	cout << "NO";
	else
	cout << "YES";
	return 0;
}
