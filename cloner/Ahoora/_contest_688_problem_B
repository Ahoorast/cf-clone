#include <bits/stdc++.h>
using namespace std;
 
const int x = 1378;
 
int main() {
 	ios_base:: sync_with_stdio(false), cin.tie(0), cout.tie(0);
	string s;
	cin >> s;
	int sz = s.size();
	cout << s;
	for (int i = sz - 1; i >= 0; i--)
		cout << s[i];
	return 0;
}
