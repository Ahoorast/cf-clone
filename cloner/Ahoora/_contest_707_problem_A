#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n, m, cnt = 0;
	char c;
	cin >> n >> m;
	for(int i = 0; i < n * m; i++)
	{
		cin >> c;
		if(c == 'C' || c == 'M' || c == 'Y')
		cnt++;
	}
	if(cnt == 0)
	cout << "#Black&White";
	else
	cout << "#Color";
	return 0;
}
