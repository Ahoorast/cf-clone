#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int a, b;
	cin >> a >> b;
	if ((a == b || a + 1 == b || a - 1 == b) && !(a == 0 && b == 0))
		cout << "YES";
	else
		cout << "NO";
	return 0;
}
