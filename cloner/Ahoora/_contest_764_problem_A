#include <bits/stdc++.h>
int arr[200];
using namespace std;
int main()
{
	unsigned long long m, n, z, ans = 0;
	cin >> m >> n >> z;
	for(int i = 1; m * i <= z; i++)
	if((m * i) % n == 0)
	ans++;
	cout << ans;
	return 0;
}
