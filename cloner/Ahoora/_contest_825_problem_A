#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int n, ans = 0;
	string s;
	cin >> n >> s;
	int sz = s.size();
	for (int i = 0; i < sz; i++)
		(s[i] == '0'? ans *= 10: ans++);
	cout << ans;
	return 0;
}
