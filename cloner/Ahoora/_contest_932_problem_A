#include <bits/stdc++.h>
using namespace std;
 
int main()
{
	string s;
	cin >> s;
	int sz = s.size();
	cout << s;
	for(int i = 0; i < sz / 2; i++) swap(s[i], s[sz - i - 1]);
	cout << s;
	return 0;
}
