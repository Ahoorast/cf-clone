#include <bits/stdc++.h>
using namespace std;
 
int main() {
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int a, b, c, n,  t;
	cin >> a >> b >> c >> n;
	t = a + b - c;
	if (t >= 0 && t < n && c <= a && c <= b)
		cout << n - t << endl;
	else
		cout << "-1" << endl;
	return 0;
}
