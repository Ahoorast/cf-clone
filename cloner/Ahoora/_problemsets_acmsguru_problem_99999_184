#include<bits/stdc++.h>
using namespace std;
 
int main() { 
	ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	int a, b, c, x, y, z;
	cin >> a >> b >> c >> x >> y >> z;
	cout << min({a / x, b / y, c / z}) << '\n';
	return 0;
}
